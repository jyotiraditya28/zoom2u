<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zoom2u">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zoom2u">
        <activity android:name=".ui.details_base_page.profile.change_price.PriceChangeActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity1"
            android:label="@string/title_activity_maps1" />
        <activity android:name=".ui.bid_request.active_bid_request.active_bid_page.ActiveBidActivity" />
        <activity android:name=".ui.bid_request.BidRequestActivity" />
        <activity android:name=".ui.qutote_request.QuoteRequestActivity" />
        <activity android:name=".ui.chat.MessageActivity" />
        <activity android:name=".ui.chat.ChatActivity" />
        <activity android:name=".ui.details_base_page.history.history_details.HistoryDetailsActivity" />
        <activity android:name=".ui.log_in.LogInActivity" />
        <activity android:name=".ui.sign_up.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.details_base_page.home.pricing_payment.PricingPaymentActivity" />
        <activity android:name=".ui.details_base_page.home.map_page.MapActivity" />
        <activity android:name=".ui.details_base_page.home.delivery_details.DeliveryDetailsActivity" />
        <activity android:name=".ui.details_base_page.BasePageActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>